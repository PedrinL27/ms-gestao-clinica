services:
  postgres:
    image: postgres:17
    container_name: gestao-clinica
    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - ./data:/var/lib/postgresql/data

